import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  defaultText: {
    fontSize: 20,
  },
  defaultTextView: {
    alignItems: 'center',
    alignSelf: 'center',
    marginTop: 50,
  },
});
