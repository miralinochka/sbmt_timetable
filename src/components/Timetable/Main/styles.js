import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
  },
  defaultText: {
    fontSize: 20,
  },
  defaultTextView: {
    alignItems: 'center',
    alignSelf: 'center',
  },
});
